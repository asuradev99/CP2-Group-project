class player{
    let x;
    let y;  
    let speed;
    let smoothSpeed = 1;
    let scl;
    const count = 3;
    let iToTheta;
    
    constructor(playername, x, y, speed, smoothSpeed, scl, count, iToTheta) {
        let x = width
    }
    
}